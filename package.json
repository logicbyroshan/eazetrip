{
  "name": "eazetrip-clone",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm run dev -- --host 0.0.0.0 --port 4174",
    "server": "node server/index.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm run build",
    "test": "node --test tests/server.test.js",
    "start": "node server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "razorpay": "^2.9.8"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
